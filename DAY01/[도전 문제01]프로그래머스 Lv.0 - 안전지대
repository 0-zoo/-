# https://school.programmers.co.kr/learn/courses/30/lessons/120866
# 양의 정수 n이 매개변수로 주어진다.
# n × n 배열에 1부터 n2 까지 정수를 인덱스 [0][0]부터 시계방향 나선형으로 배치한 이차원 배열을 return 하는 solution 함수를 작성하라.

def solution(n):
    answer = [[0 for _ in range(n)] for _ in range(n)]
    dr = [-1, 0, 1, 0]
    dc = [0, 1, 0, -1]
    r = c = 0
    d = 1
    num = 1
    answer[r][c] = num

    while True:
        if(num>=n*n): break
        num += 1
        a = r + dr[d]
        b = c + dc[d]
        if 0 <= a < n and 0 <= b < n and answer[a][b] == 0:
            r = a
            c = b
            answer[r][c] = num
        else:
            d = (d + 1) % 4
            num -= 1

    return answer
